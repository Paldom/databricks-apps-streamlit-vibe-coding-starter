---
description: "DEPRECATED: Use /agents-init instead"
allowed-tools: ["Bash", "Read", "Write", "Edit", "Glob", "Grep"]
---

# /scaffold (Deprecated)

> **This command has been renamed to `/agents-init`.**
> Please use `/agents-init` instead.

This is a compatibility shim. The functionality has moved to the `/agents-init` command.

## Quick Migration

Instead of `/scaffold`, use:

```
/agents-init
```

Or run the CLI directly:

```bash
npx -y agents-init@latest
```

---

*Powered by [agents-init](https://github.com/Paldom/agents-init)*
